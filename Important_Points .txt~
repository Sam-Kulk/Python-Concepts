- "" == '' in python
- In Python for boolean value True & False, the numeric values 1 & 0 will be assigned by defalut, respectively.
- In python the +ve indexing starts from '0', negative indexing starts from '-1' and length count starts from '1'.
- In python blocks cannot be empty, as a workaround 'pass' statement can be used.
- In python list are used as an array, there is no seperate built-in array as such.
- function/method name can have alphabets(L & U), numbers & underscore, but what's recomended is that I need to use lowercase only one word & if more words then use in snake case.
- The class name should be PascalCase
- In Python, there is no datatype for date, but I can work with datetime by importing datetime module.
- All built-in modules classes have usually static methods and can be accessed with ClassName.methodNmae()
- In Python comma(,) or + used as a seperator, to seperate String & variable of data str.
- In python '.' is used for navigation between different modules.
- Whenever I want to work with any RDBMS, there will be a dedicated package or say driver of that which it, that need to install & make use of.
    Ex for MySQL - mysql-connector-python package I need to install
