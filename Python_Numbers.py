import random

# Example 1
print(random.randrange(1,3)) # This can generate same number multiple times, as each call here will be independent of the other
# Note : since range is there, end point-1 will be considered

